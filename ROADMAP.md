# jisilu-deck Roadmap

集思录页面增强 Chrome 插件：在可转债列表原操作按钮旁增加橙色 `+`／红色 `-`，维护浏览器本地自选。

## 当前阶段

第一版完成并验收（2026-08-24）：单元测试 11/11、Playwright 真实页面 E2E 29/29、browser-skill 驱动散帅真实 Chrome（登录态、311 行全量视图）实测加入／刷新持久化／移出恢复全部通过。第一版正式收尾。

## 想法来源（自 `PROJECT_IDEAS.md` 迁入，2026-08-17 立项）

- **原始想法**：开发独立的网页端集思录页面增强插件，在浏览集思录时显示个人当前持仓和自选可转债。
- **背景**：散帅不准备续费集思录会员；非会员目前只能添加 5 只自选可转债，但个人仍需要维护更多自选标的。
- **原边界**：持仓与自选是两类数据，后续需分别确认数据维护方式。实现不以绕过会员权限或访问控制为目标。

## 决策记录

- **2026-08-17 立项**：
  - 项目名定为 `jisilu-deck`（deck = 操作面板，在集思录页面上叠一块个人面板）。
  - 形态确认为 **Chrome 浏览器插件**（Manifest V3），作用于集思录网站页面。
  - 与 `kzz-radar` 保持独立：两者项目定位不同（radar 做策略研究与决策辅助，deck 做浏览时的个人数据展示），不共享代码与数据。
  - 权限红线：不以绕过集思录会员权限或访问控制为目标；不抓取需登录/付费才能访问的接口数据。

## 已完成

- 2026-08-17：立项，创建项目目录与规则文件（`CLAUDE.md`、`AGENTS.md`、`ROADMAP.md`），想法条目自 `PROJECT_IDEAS.md` 迁入本项目，已在 `WORKSPACE.md` 登记。
- 2026-08-22：补充 `README.md`、目录约定、v0 产品定义和实施前调研清单；当时建议采用 `chrome.storage.local` 手动维护、页面内浮动抽屉和页面代码匹配，页面读取方案后因协议调研结论调整。
- 2026-08-22：散帅确认第一版采用 `chrome.storage.local` 手动维护与页面内浮动抽屉，目标页确认为 `https://www.jisilu.cn/web/data/cb/list`。
- 2026-08-22：完成目标页面、集思录服务协议与 Chrome 官方能力首轮调研。根据协议禁止自动化抓取的新增证据，第一版收窄为只展示本地数据，不读取或解析集思录页面内容。
- 2026-08-22：补充第一版数据规则、最小权限、URL 范围、抽屉生命周期与样式隔离文档；未创建 Manifest、源码或测试。
- 2026-08-24：散帅确认持仓数量使用“张”且只允许正整数，成本价使用“元/张”并显示 3 位小数；数据规则正式定案。
- 2026-08-24：补充第一版界面交互规格与后续验证计划；当前只定义完成标准，未创建或执行插件测试。
- 2026-08-24：未登录 Edge 会话直接打开目标 URL，确认页面正常显示“数量 30/30个”；纠正此前“需要登录”的判断，并根据 `1192×686` 视口和横向宽表实测确定右侧覆盖式抽屉尺寸。
- 2026-08-24：散帅将第一版收敛为操作列行内本地自选按钮，废止持仓、抽屉、表单和其他功能设计；橙色 `+` 加入，红色 `-` 移出，不提供批量操作。
- 2026-08-24：只读核对原操作按钮：操作单元格宽 `32px`，原图标约 `13×13px`，原 `+` 为深蓝色 `rgb(32, 103, 152)`；散帅补充确认原 `-` 为红色。本地按钮使用普通文本，不复用集思录图标字体。
- 2026-08-24：最终交互确认为只保留橙色 `+` 和红色 `-`：`+` 加入当前转债，`-` 移出当前转债；橙色 `+` 与原站深蓝色 `+` 区分，本地红色 `-` 允许与原站红色 `-` 同色；不增加文字状态、批量操作或其他功能。
- 2026-08-24：加入本地自选成功后，当前行转债名称同步变为与本地 `-` 相同的红色；移出成功后恢复页面原本颜色，不改变代码和其他字段样式。
- 2026-08-24：散帅授权进入实现。完成目标页 DOM 结构与选择器二次调研（`docs/research/2026-08-24-cb-list-dom-structure.md`），确认主表识别方式、操作格几何（宽 `32px`、padding `0 1px`、sticky 定位）与代码/名称单元格选择器。
- 2026-08-24：TDD 实现存储层 `src/content/watchlist-store.js`（先写 11 个单元测试再实现，`node --test` 全绿），覆盖去重、校验、trim、幂等删除与存储失败语义。
- 2026-08-24：实现 `manifest.json`、`src/content/page-adapter.js`、`src/content/main.js`；本地按钮绝对定位注入操作格，`MutationObserver` 监听表格重渲染，`storage.onChanged` 统一重放状态（含跨标签页同步）。E2E 发现并修复内容脚本在表格渲染前退出的问题（`document_idle` 时表未渲染，改为轮询等待主表出现）。
- 2026-08-24：E2E 验收 29/29 通过：注入唯一性、不扩列宽、不覆盖原按钮、颜色语义、名称标红与恢复、移出隔离、页面刷新/排序重渲染/浏览器重启持久化、全程无 `chrome-extension://` 发起请求；截图视觉复核通过。E2E 脚本收入仓库 `tools/e2e-cb-list.js`（不放在 `test/` 下，避免被 `node --test` 误当单元测试执行）。

## 待办

无。后续如有需求再开新版本（如本地自选管理、导出等，均不在第一版范围）。

## 阻塞

无。

## 最近验证

- 2026-08-17：立项当日无代码，未涉及验证。
- 2026-08-22：完成文档交叉检查；产品定义中的未验证外部事实均保留“待确认”标记。本轮无代码，不涉及插件运行验证。
- 2026-08-22：直接 HTTP 请求目标 URL 返回 Vue 单页应用壳；未登录 Edge 会话打开后回到首页。核对集思录首页目标链接、官方服务协议、Chrome Storage API 与内容脚本官方文档。本轮只更新文档，未创建或运行插件代码。
- 2026-08-22：检查全部项目文件清单、文档入口与 `git diff --check`，结果通过；仓库中不存在 Manifest、源码或测试文件。
- 2026-08-24：完成数据规则、界面规格和验证计划的文档交叉检查；本轮未创建或运行插件代码。
- 2026-08-24：重新验证目标页，最终 URL 为 `https://www.jisilu.cn/web/data/cb/list`，标题为“列表 - 可转债 - 集思录”，未登录页面显示“数量 30/30个”。记录视口与布局几何，不提取或保存债券字段和行情内容。
- 2026-08-24：核对第一行操作单元格的最小 DOM 与计算样式，未点击原按钮、未改变集思录站内自选；据此完成本地 `+`／`-` 的视觉区分规范。
- 2026-08-24：完成单一 `+`／`-` 功能的产品、数据、界面、实施边界和验证文档一致性检查；本轮未创建或运行插件代码。
- 2026-08-24：按最终颜色语义复查全部文档：本地 `+` 为橙色、本地 `-` 与已选转债名称为同一红色；`git diff --check` 通过，仓库中仍无 Manifest、源码或测试文件。
- 2026-08-24：实现后验证——`node --test` 11/11 通过；Playwright 加载未打包扩展在真实目标页运行 `tools/e2e-cb-list.js`，29/29 通过（含页面刷新、表头排序重渲染、浏览器重启持久化、无插件发起请求）；整页截图视觉复核按钮并列、无重叠溢出、已选名称红色。
- 2026-08-24：browser-skill 驱动散帅真实 Chrome（登录态）验收：散帅在 `chrome://extensions` 手工加载未打包扩展后，311/311 数据行注入且无重复；110092 加入→红 `-` 与名称同红、刷新后状态恢复、移出后橙 `+` 与名称原色恢复、原按钮全程未动；验收测试数据已清理。
