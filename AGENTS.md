# jisilu-deck · Codex 项目规则

本文件是 Codex 在 `jisilu-deck` 项目内的行为约束。内容与 `CLAUDE.md` 保持同步，供不同工具读取。冲突时以 `CLAUDE.md` 为准（红线与安全约束除外）。

## 项目身份

- 名称：`jisilu-deck`
- 定位：集思录页面增强 Chrome 插件——在可转债与 QDII 列表操作列增加本地自选按钮，突破站内自选数量对个人记录的限制
- 散帅：bni
- workspace 内路径：当前仓库根目录
- 同级相关项目：`kzz-radar`（可转债研究与决策辅助，两者定位不同，互不归属）；`WhiteHorseWeb`（投资管理系统）

## 启动读取顺序

进入本项目时按顺序读：

1. `../WORKSPACE.md`
2. `CLAUDE.md`（与 Claude Code 共用，内容保持同步）
3. 本文件（`AGENTS.md`）
4. `ROADMAP.md`
5. 与当前任务直接相关的 `docs/` 文档

## 项目边界（立项时确认）

- **形态**：Chrome 浏览器插件（Manifest V3），作用于集思录网站页面。
- **当前范围**：维护本地自选可转债，以及相互隔离的欧美市场、商品、亚洲市场三类 QDII 本地自选；不包含持仓、行情、评分或投资建议。
- **权限红线**：实现不以绕过集思录会员权限或访问控制为目标；不抓取需登录/付费才能访问的接口数据。
- **与 `kzz-radar` 的关系**：独立项目，不共享代码与数据；`kzz-radar` 面向策略研究与决策，本插件面向浏览集思录时的个人数据展示。

## 技术约定

- 代码、命令、变量名、文件名用英文；文档与注释默认中文。
- 新增依赖前先检查项目已有依赖与 Chrome 平台原生能力（storage、sidePanel 等 API）。
- 涉及集思录表格结构的选择器解析逻辑集中在独立模块，页面改版时只改该模块。

## 目录约定

- `goals/`：保存可独立实施和验收的施工目标；文件名使用三位顺序号加英文短横线命名，如 `001-cb-purchase-queue.md`。Goal 完成后保留作为范围与验收记录，不自动删除。
- `docs/product/`：产品定位、范围、用户流程、验收标准等稳定产品定义。
- `docs/architecture/`：权限边界、数据持久化、界面生命周期和模块责任等实施前技术设计；不得在文档阶段创建对应代码骨架。
- `docs/research/`：集思录页面、公开数据、使用条款与 Chrome 能力的调研记录；未经验证的信息必须标记为“待确认”。
- `docs/assets/`：README 和公开文档长期使用的脱敏图片；文件名使用英文小写加连字符，并在引用处说明用途。
- `src/`：插件源码；`src/content/` 为内容脚本（存储层、页面适配层、主逻辑）。
- `test/`：单元测试（`node --test` 默认会执行该目录下文件，只放单元测试）。
- `tools/`：开发与验证辅助脚本（如 E2E 验收），不属于插件运行时代码。
- `release/`：本地生成的 GitHub Release 发布包；目录整体忽略，不提交 Git，发布包可按对应 tag 重新生成。
- 临时截图、页面快照和实验文件不进入仓库；确需长期保留时先在 `docs/research/` 说明来源与用途。

## 验证要求

- 插件功能改动后，先在 Chrome `chrome://extensions` 重新加载未打包扩展，再用 browser-skill 连接真实 Chrome 实测，不只跑单测。
- 每轮验证在 `ROADMAP.md` 记录验证方式与结果。
- 浏览器验证及相关工具任务结束前，必须清理本任务创建的全部运行态和临时状态：停止所有 session、关闭 Agent Window、归还借用标签页、恢复测试数据、停止因本任务启动的 `bsk daemon` 等后台进程，并清理本任务产生且允许自动清理的临时文件和临时配置；用户明确要求保留的持久配置除外。
- Chrome 顶部不得残留“BrowserSkill 已开始调试此浏览器”等调试附加提示；窗口关闭或 `active sessions = 0` 不能替代现场确认。只要该提示仍存在，就视为浏览器控制未清理完成。
- 清理后必须使用 `bsk session list` 或对应工具的状态命令复核，不得只依据脚本退出或窗口不可见判断已经清理。
- 如果 `browser-skill` 等上层工具规则禁止 Agent 手动管理 daemon，或某项残留因权限、外部状态无法清理，不得宣称任务已经完成；必须明确说明残留对象、影响和所需处理方式，等待用户处理、工具自动回收或规则允许的授权路径。
